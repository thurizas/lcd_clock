#pragma once

const int   major = 1;
const int   minor = 2;
const int   patch = 0;

// this will be incremented on each release build
const int   build = 4;

// this will be incremented on each release build
const char*  buildTime[] = { "27Aug2022 1140" };
