#pragma once

const int   major = 1;
const int   minor = 1;
const int   patch = 0;

// this will be incremented on each release build
const int   build = 3;

// this will be incremented on each release build
const char*  buildTime[] = { "08Feb2010 1000" };
